

=Install UUID
npm install uuid // under expressserver

eg: w;ith destructuring
const {v4:uuidv4} = require('uuid')

eg: without destructuring
const uuid = requuire('uuid')
const uuid = uuid.v4
-------------------------------------------
=>Install Bootstrap 

npm install bootstrap @popperjs/core
     (i) Import Bootstrap CSS in your main.jsx or App.jsx
     import "bootstrap/dist/css/bootstrap.min.css"
     import "bootstrap/dist/js/bootstrap.bundle.min.js"

=>Install Fontawesome (v ^6.7.2, v ^0.2.2)
     https://docs.fontawesome.com/web/use-with/react

     (i) install 
          npm i --save @fortawesome/fontawesome-svg-core
          npm i --save @fortawesome/react-fontawesome@latest

          npm i --save @fortawesome/free-solid-svg-icons
          npm i --save @fortawesome/free-regular-svg-icons
          npm i --save @fortawesome/free-brands-svg-icons
     (ii) Create a Fontawesome config file
          src > libs > fontawesome.js


          import { library } from '@fortawesome/fontawesome-svg-core'
          import { fab } from '@fortawesome/free-solid-svg-icons'
          import { far } from '@fortawesome/free-solid-svg-icons'
          import { fas } from '@fortawesome/free-solid-svg-icons'

         
          library.add(fab, far, fas)
     (iii) Import Config to src/main.jsx
          src > main.jsx
          
          import './libs/fontawesome.js'

     (iv) Usage in Componnts
          import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'

=>Install jQuery and Inject (v, v)
     https://www.npmjs.com/package/@rollup/plugin-inject
     (i) instal jQuery 
          npm install jquery 

     (ii) install Inject
          npm install @rollup/plugin-inject --save-dev

     (iii) Vite config setting for Inject 
          // vite.config.js


          import { defineConfig } from 'vite'
          import react from '@vitejs/plugin-react'

          // https://vite.dev/config/
          export default defineConfig({
          plugins: [
          react(),
          inject({
               $: 'jquery',
               jQuery: 'jquery'
          })
          ],
          optimizeDeps: {
          include : ['jquery']
          }
          })

     (iv) Import jquery to your component
          import $ from jquery;
          useEffect(()=>{
               $('.titles').css('color',"red")
          })

=> Install lightbox 2 (v)

     (i) install 
          npm install lightbox2 --save

     (ii)  Import 
          // src/main.jsx     or   your Components

          import 'lightbox2/dist/css/lightbox.min.css'
          import 'lightbox2/dist/js/lightbox.min.js'


 => React-Tostify (v ^11.0.5)
 //        https://www.npmjs.com/package/react-toastify
//        https://fkhadra.github.io/react-toastify/introduction

     (i) install
          $ npm install --save react-toastify

     (ii) Import
          // src/main.jsx     or   your Components
          import { ToastContainer, toast } from 'react-toastify';
          
          <ToastContainer />

=> Stripe Payment Gateway
     https://stripe.com/

     (i) Install (in expressapi) (^19.1.0)
     https://stripe.com/ > Developers > Libraries and SDKs > Developer Resources > Essentials , SDKs > Overview
     > Stripe Node Library

     or

      https://stripe.com/ > Dashboard > Developers > Libraries and SDKs > Developer Resources > Essentials , SDKs > Overview
     > Stripe Node Library

     npm install stripe


     (ii) Import server.js
     const Stripe = require("stripe");

     (iii) Config Route
          https://stripe.com/ > Developers > Full API reference > Core Resources > Payment Intents > Create a PaymentIntent

     (iv) Install (in my-app)
        https://stripe.com/ > Developers > Libraries and SDKs > Developer Resources > Essentials , SDKs > React Strip.js

        npm install --save @stripe/react-stripe-js @stripe/stripe-js


     https://stripe.com/ > Dashboard > Developers > Libraries and SDKs > Developer Resources > Essentials , SDKs > React Stripe.js

=> multer (v ^2.0.2)
     https://www.npmjs.com/package/multer

     (i) install 
     npm i multer

=> Deploy
     => render.com (express)

     => app.netify.com (Landing Page)
     
------------------------------------------------------------------------------------------------------------------------
     =>🧩 Imagine a real-world example first

               1️⃣ You tell the waiter (React app):

               “I want to pay $30 for my meal.”

               2️⃣ The waiter doesn’t have access to the cash register (Stripe Secret Key).
               So the waiter goes to the cashier (your backend/Node.js) and says:

               “The customer wants to pay $30. Please prepare the bill.”

               3️⃣ The cashier creates an official bill slip and gives it back to the waiter.
               That bill slip is your client_secret.
               It’s a proof that the cashier approved a $30 payment.

               4️⃣ The waiter (React frontend) takes that bill slip to the payment terminal (Stripe) and swipes the card.
               The terminal verifies the slip and the card, then charges the customer.

               ✅ Why do we do this?

               Because:

               The cashier (backend) is the only one who can create official payment requests.

               The waiter (frontend) is the only one who interacts with the customer’s card.

               The client_secret connects the two.

               🧠 Now, the technical reason

               Let’s go deeper, but still simply.

               1️⃣ Backend must create the payment first

               Only your backend has your Stripe Secret Key (sk_test_xxx).
               That key has full control over your Stripe account — it can create, refund, or cancel payments.
               If you put that key in React, anyone could steal it and charge people 😱

               So your backend does the safe part:

               const paymentIntent = await stripe.paymentIntents.create({
               amount: Math.round(amount * 100),
               currency: "usd",
               });


               This tells Stripe:

               “I want to charge $29.99, please prepare for a card payment.”

               Stripe creates a record internally and returns a client_secret:

               {
               "client_secret": "pi_3Nx7c8_secret_abc123"
               }

               2️⃣ The frontend gets the client_secret

               The client_secret is a safe one-time token —
               it tells Stripe:

               “This specific payment is authorized by the backend.
               You can now accept card details for it.”

               But it cannot create, cancel, or refund payments.
               So it’s safe to use in the frontend.

               3️⃣ Frontend confirms the payment directly with Stripe

               Now the frontend (React) calls:

               stripe.confirmCardPayment(clientSecret, {
               payment_method: {
               card: elements.getElement(CardElement)
               }
               });


               This step sends:

               The client_secret (permission slip from backend)

               The card details (entered by the customer)

               directly to Stripe’s servers — not to your backend.

               Then Stripe checks:

               Is this client_secret valid?

               Is the card info valid?

               If both are OK → Stripe completes the charge.

               ⚡️ Why not send card info to backend?

               Because card data is highly sensitive.
               If you send it through your backend:

               You must store, encrypt, and protect it (PCI-DSS compliance — very complex and expensive).

               If your server is hacked, attackers could get card numbers.

               By using Stripe’s frontend SDK (CardElement, confirmCardPayment):

               Card data goes directly from browser → Stripe (secure HTTPS).

               Your backend never touches it.

               You stay 100% PCI-compliant automatically.

               🔐 So in short:
               Step	Who does it	What it does	Why it’s needed
               1️⃣	Backend (Node)	Creates PaymentIntent, gets client_secret	Only backend can safely use secret key
               2️⃣	Frontend (React)	Uses client_secret to confirm payment with card info	Only frontend can collect card info
               3️⃣	Stripe	Matches both and charges the card	Secure + verified
               🧭 Simple summary

               👉 client_secret = a one-time “ticket” that says “backend approved this payment”.
               👉 React uses that ticket + card info to tell Stripe “go ahead and charge it”.
               👉 Stripe does the charge safely — your backend never sees the card.






-------------------------------------------------------------------------------------------------
     🔧 1. @fortawesome/react-fontawesome — React Component Library
          ✅ Purpose:
          This is the React wrapper that allows you to use Font Awesome icons in your JSX/React components using a <FontAwesomeIcon /> tag.

          🔍 What’s inside?
          A React component: FontAwesomeIcon

          Takes JavaScript objects (icons) and renders them into SVG icons in your React DOM

          Handles props like icon, size, color, spin, etc.

          🔨 Example:
          js
          Copy
          Edit
          import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
          import { faCoffee } from '@fortawesome/free-solid-svg-icons';

          function MyComponent() {
          return <FontAwesomeIcon icon={faCoffee} size="lg" color="brown" spin />;
          }
          ➡️ This component internally uses the fontawesome-svg-core to render the SVG.

          🧠 2. @fortawesome/fontawesome-svg-core — Core Engine
          ✅ Purpose:
          This package is the core rendering engine of Font Awesome. It does not care about React or any UI framework.

          🔍 What’s inside?
          Functions and utilities to manage icons

          Converts icon definitions into SVG elements

          Manages the icon library

          Enables things like:

          library.add() to globally register icons

          Configuration of class names, CSS injection

          Manual DOM rendering (non-React usage)

          🔨 Example:
          js
          Copy
          Edit
          import { library } from '@fortawesome/fontawesome-svg-core';
          import { faCoffee } from '@fortawesome/free-solid-svg-icons';

          library.add(faCoffee); // Register icon globally
          ➡️ This library does not render anything itself. It prepares the data needed to render.

